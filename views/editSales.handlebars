<div class="row-list">
<h1>Edit Sale</h1>

<form action="/sales/update/{{data.id}}" method="POST">
	<div class="">
		<!-- <label for="">Category</label> -->

    <!-- <select class="input-field" name="category_id"> -->
    <!-- </select> -->
	</div>
  <div>
		<h4>date</h4>
		<input class="datepicker" type="date" name="date" value="{{data.date}}">
	</div>
  <div>
		<h4>quantity</h4>
		<input type="number" name="sold" value="{{data.sold}}">
	</div>
  <div>
		<h4>cost</h4>
		<input type="text" name="price" value="{{data.price}}">
	</div>
  <h4>Product</h4>
  <select class="browser-default" name="product_id">
    {{#sales}}
    <option value="{{id}}"> {{product}}</option>
    {{/sales}}
  </select>


	<input
		<button type ="submit" class="btn btn-primary"> </button>

</form>
</div>
